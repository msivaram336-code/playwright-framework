pipeline {
    agent any

    stages {

        stage('Install Dependencies') {
            steps {
                dir('playwright-framework') {
                    bat 'npm install'
                }
            }
        }

        stage('Run Playwright Tests') {
            steps {
                dir('playwright-framework') {
                    bat 'npx playwright test'
                }
            }
        }
    }
}
